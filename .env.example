# Example environment variables for docker-compose / the agent service
# Copy this file to `.env` and fill in the JENKINS_SECRET value (do NOT commit secrets).

# Agent name used by the inbound agent service (matches the node name you create in Jenkins)
JENKINS_AGENT_NAME=agent-1

# Agent JNLP secret/token (obtain from Jenkins -> node -> Launch agent by connecting it to the controller)
JENKINS_SECRET=

# Optional: override controller URL (defaults to http://jenkins:8080 inside the compose network)
#JENKINS_URL=http://localhost:8080
